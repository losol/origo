{
  "name": "origo",
  "version": "0.0.0",
  "private": true,
  "description": "Shared foundation for the Eventuras ecosystem — build configs and core utilities",
  "repository": {
    "type": "git",
    "url": "https://github.com/losol/origo.git"
  },
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "lint": "pnpm -r --if-present run lint",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  },
  "engines": {
    "node": ">=24.0.0 <25.0.0",
    "pnpm": ">=11.0.0"
  }
}
